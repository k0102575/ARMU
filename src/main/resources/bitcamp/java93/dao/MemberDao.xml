<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="bitcamp.java93.dao.MemberDao">
  
  <resultMap type="member" id="memberMap">
    <id column="mno" property="no"/>
    <result column="name" property="name"/>
    <result column="phone" property="phone"/>
    <result column="pwd" property="password"/>
    <result column="email" property="email"/>
    <result column="path" property="photo"/>
  </resultMap>

  <sql id="select1">
    select mno, name, phone, email 
    from memb
  </sql>

  <select id="selectList" resultMap="memberMap">
    <include refid="select1"/>
    order by name asc 
  </select>
  
  <insert id="insert" parameterType="member" 
          useGeneratedKeys="true" keyColumn="mno" keyProperty="no">
    insert into memb(name,phone,pwd,email) 
    values(#{name},#{phone},password(#{password}),#{email})
  </insert>
  
  <select id="selectOneByEmailPassword" resultMap="memberMap" parameterType="map">
    select  m.email, m.mno, m.name, m.phone, m.path
    from memb m
    where email=#{email} and pwd=password(#{password})
  </select>

  <select id="selectOne" resultMap="memberMap">
    select mno, name, path, pwd, email,phone from memb
    where mno=#{no}
  </select>

  
  <update id="updateNick" parameterType="member">
    update memb set 
      nick=#{nickName}
    where mno=#{no}
  </update>
  
  <update id="updatePwd" parameterType="member">
    update memb set 
      pwd=password(#{password})
    where mno=#{no}
  </update>

  <update id="updatePhoto" parameterType="map">
    update memb set
    path=concat('/image/profile/',#{photoPath},'_140.png')
    where mno=#{memberNo}
  </update>
</mapper>




